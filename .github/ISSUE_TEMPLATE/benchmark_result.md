---
name: Benchmark result
about: Share a run of compare / sweep / sweep3d (yours or a reproduction)
labels: benchmark
---

**Command**

```bash
moss-reg compare --type ...   # exactly as run
```

**`report.json` entry** (paste the entry for this command: commit, environment, params, metrics, checks)

**Figure / CSV** (attach the PNG and, if relevant, the per-step CSV)

**What you conclude, and what the damping number was**
